<%= form_with(model: [@account, @course, @assignment], local: true) do |form| %>
  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, placeholder: "Resolver ejercicios taller" %>
  </div>
  
  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, placeholder: "Deberá resolver los ejercicios", rows: 5 %>
  </div>

  <div class="actions">
    <%= form.submit 'Crear tarea' %>
  </div>
<% end %>
